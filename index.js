const currentUser = 'Tracy Kagiri';

const welcomeMessage = `Welcome to Flatbook, ${'Tracy Kagiri'}!`;

const excitedWelcomeMessage = welcomeMessage.toUpperCase();

const shortGreeting = `Welcome, ${'Tracy Kagiri'.slice(0, 1)}!`;













